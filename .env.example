# MCP Server Configuration
PORT=3000
NODE_ENV=production

# SSL Wireless API base URL (default is the production gateway)
SSLW_BASE_URL=https://smsplus.sslwireless.com

# NOTE: api_token and sid are NOT configured here.
# They are passed by each caller as tool arguments at runtime,
# so a single deployed server can serve multiple tenants/applications.
